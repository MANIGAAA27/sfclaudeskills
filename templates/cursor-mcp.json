{
  "mcpServers": {
    "salesforce-dx": {
      "command": "npx",
      "args": [
        "-y",
        "@salesforce/mcp",
        "--orgs",
        "YOUR_DEFAULT_ORG_ALIAS",
        "--toolsets",
        "orgs,metadata,data,users",
        "--allow-non-ga-tools"
      ]
    }
  }
}
